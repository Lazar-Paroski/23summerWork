These are some common git commands that I know how to use.(test)

- git config --global user.name "your name"

- git config --global user.email "your@email.com"

- cd c:/users/lazar/desktop/fileName  (specifying branch)

- git init (initializing repository)

- git status

- git add fileName.txt

- git add .  (adds all documents to staging)

- git commit -m "insert message here"  (commits documents)

- git log

- git checkout commit hash  (lets you go back to a previous commit)

- git chackout master   (lets you return to master)

- git branch DeveloppmentBranchName  (making a new branch)

- git checkout branch-name

- git checkout -b New-branch-name  (makes and switches to the branch at the same time)

- git merge branch-name  (this merges the branch indicated with the main branch)

- git branch -d branch-name   (this deletes the named branch)


 